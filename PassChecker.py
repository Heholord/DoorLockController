import RPi.GPIO as GPIO
import time


class PassChecker:
    def __init__(self):
        self.pass = false

    def read(self, pathToPass):

    def compare(input, pass):
        return pass.compare(input)

    if compare(keypad, password)
        open_door()

while(True):
    keypad = Keypad()
    keypad.read()
    time.sleep(0.1)
